namespace Ahmad.Mafia.Domain.Room.Enums;

public enum RoomStatus
{
    WaitingForPlayers = 0,
    ReadyToStart = 1,
    InProgress = 2,
    Closed = 3,
}

/// <summary>
/// عمومی: هرکسی با «بازی سریع» وارد صف matchmaking می‌شود و به یکی از روم‌های
/// عمومیِ در انتظار وصل می‌شود (یا اگر نبود، یکی ساخته می‌شود) — پر شدن ظرفیت
/// خودش یعنی شروع خودکار بازی، چون میزبانی برای کاربر بی‌معناست.
/// خصوصی: سازنده لینک/کد روم را برای دوستانش می‌فرستد و خودش با کلیک روی
/// «شروع بازی» تصمیم می‌گیرد بازی کی شروع شود.
/// </summary>
public enum RoomVisibility
{
    Private = 0,
    Public = 1,
}

/// <summary>
/// هر سناریو یه rule-engine جدا برای نقش‌ها و شرط بردنه (سند ۰۱). سناریوهای بیشتر
/// (زودیاک، قمارباز و ...) بعداً به همین enum اضافه می‌شن، نه هاردکد جای دیگه.
/// </summary>
public enum ScenarioType
{
    /// <summary>مافیای روسی — v1: فقط شهروند ساده + مافیای ساده، کشتن فقط شب اول.</summary>
    RussianMafia = 0,
    /// <summary>شب‌های مافیا — پدرخوانده/دکتر/کارآگاه، اکشن شب هر راند.</summary>
    MafiaNights = 1,
    /// <summary>انتخابات شهر — یک شهروند «شهردار» می‌شه که رأیش روز دو برابر حساب می‌شه.</summary>
    MayorElection = 2,
    /// <summary>محافظ سایه — یک «بادیگارد» هر شب از یک نفر محافظت می‌کنه؛ اگه مافیا همون رو بزنه، به‌جای هدف، خودِ بادیگارد کشته می‌شه.</summary>
    ShadowGuard = 3,
    /// <summary>شکار روانی — یک «قاتل زنجیره‌ای» مستقل هم‌زمان با مافیا شب‌ها دست به کشتن می‌زنه و هدفش تنها ماندنه، نه پیروزی تیمی.</summary>
    SerialHunt = 4,
}
